/**
 * Jumlah item manhwa yang ditampilkan per halaman
 * Digunakan untuk pagination
 */
export const ITEMS_PER_PAGE = 20;

/**
 * Interval waktu untuk auto-slide hero carousel (dalam milliseconds)
 * 5000ms = 5 detik
 */
export const SLIDER_AUTOPLAY_INTERVAL = 5000;

/**
 * Jumlah manhwa rekomendasi yang ditampilkan di hero slider
 * Manhwa dipilih berdasarkan rating tertinggi
 */
export const RECOMMENDED_MANHWA_COUNT = 5;

/**
 * Delay untuk debounce search input (dalam milliseconds)
 * Mencegah tracking terlalu sering saat user mengetik
 */
export const SEARCH_DEBOUNCE_DELAY = 500;
